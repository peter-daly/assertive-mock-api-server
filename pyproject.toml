[tool.uv.workspace]
members = ["server", "client"]

[dependency-groups]
dev = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.6.15",
    "pymdown-extensions>=10.16",
    "pytest>=9.0.3",
    "ruff>=0.12.5",
    "ty>=0.0.18",
]

[tool.ty.environment]
root = ["./server", "./client", "."]
