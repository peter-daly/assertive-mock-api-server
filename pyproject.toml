[tool.uv.workspace]
members = ["server", "client"]

[dependency-groups]
dev = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.6",
    "pymdown-extensions>=10.16",
    "pytest>=8.4.1",
    "ruff>=0.12.5",
    "ty>=0.0.18",
]

[tool.ty.environment]
root = ["./server", "./client", "."]
